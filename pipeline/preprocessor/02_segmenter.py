"""
Phase 2: 형태소 기반 문장 복원 및 필터링
- Kiwi 기반 문장 분리
- 품사(POS) 태깅 기반 짧은 문장 필터링
"""

def filter_short_sentences(text: str) -> bool:
    """품사 태깅을 기반으로 유의미한 문장인지 판별합니다."""
    # TODO: kiwipiepy 적용 로직 반환
    return True

if __name__ == "__main__":
    pass
